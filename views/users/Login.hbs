<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login & Sign Up</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background: url('https://c4.wallpaperflare.com/wallpaper/649/200/63/digital-art-artwork-abstract-wallpaper-preview.jpg') no-repeat center center fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: 'Poppins', sans-serif;
            color: #fff;
            position: relative;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(30, 60, 90, 0.7);
            z-index: 1;
        }

        .auth-card {
            background: #fff;
            color: #333;
            border-radius: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            max-width: 900px;
            width: 100%;
            z-index: 2;
            position: relative;
        }

        .left-panel {
            background: linear-gradient(135deg, #c8bcd5, #011d4d);
            color: #fff;
            text-align: center;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .left-panel h1 {
            font-size: 2rem;
            margin-bottom: 1rem;
        }

        .left-panel p {
            font-size: 1rem;
            margin-bottom: 1.5rem;
        }

        .left-panel .btn {
            background: #fff;
            color: #6a11cb;
            border-radius: 30px;
            padding: 0.6rem 1.5rem;
            font-weight: bold;
        }

        .form-panel {
            padding: 2rem;
        }

        .nav-tabs {
            justify-content: center;
            margin-bottom: 1.5rem;
        }

        .nav-link {
            font-weight: bold;
            border: none;
            color: #333;
        }

        .nav-link.active {
            color: #6a11cb;
            border-bottom: 2px solid #6a11cb;
        }

        .form-control {
            border-radius: 30px;
        }

        .btn-custom {
            background-color: #6a11cb;
            color: #fff;
            border-radius: 30px;
            padding: 0.8rem 1.5rem;
            font-weight: bold;
        }

        .btn-custom:hover {
            background-color: #2575fc;
        }

        @media (max-width: 768px) {
            .auth-card {
                flex-direction: column;
            }

            .left-panel {
                padding: 1.5rem;
            }

            .form-panel {
                padding: 1.5rem;
            }
        }

        @media (max-width: 576px) {
            .left-panel h1 {
                font-size: 1.8rem;
            }

            .left-panel p {
                font-size: 0.9rem;
            }

            .btn {
                padding: 0.5rem 1rem;
                font-size: 0.9rem;
            }

            .btn-custom {
                padding: 0.7rem 1rem;
            }
        }
    </style>
</head>

<body>
    <div class="auth-card d-flex flex-md-row flex-column">
        <!-- Left Panel -->
        <div class="col-md-6 left-panel">
            <h1>Welcome Back!</h1>
            <p>Log in or sign up to explore more.</p>
            <button class="btn btn-light mt-3">Learn More</button>
        </div>
        <!-- Form Panel -->
        <div class="col-md-6 form-panel">
            <ul class="nav nav-tabs" id="authTabs" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="login-tab" data-bs-toggle="tab" data-bs-target="#login"
                        type="button" role="tab" aria-controls="login" aria-selected="true">Login</button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="signup-tab" data-bs-toggle="tab" data-bs-target="#signup" type="button"
                        role="tab" aria-controls="signup" aria-selected="false">Sign Up</button>
                </li>
            </ul>

            <div class="tab-content">
                <div class="tab-pane fade show active" id="login" role="tabpanel" aria-labelledby="login-tab">
                    <form>
                        <div class="mb-3">
                            <label for="loginEmail" class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="loginEmail" placeholder="Enter your email">
                        </div>
                        <div class="mb-3">
                            <label for="loginPassword" class="form-label">Password</label>
                            <input type="password" class="form-control" id="loginPassword"
                                placeholder="Enter your password">
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-custom">Login</button>
                        </div>
                    </form>
                </div>

                <!-- Sign Up Form -->
                <div class="tab-pane fade" id="signup" role="tabpanel" aria-labelledby="signup-tab">
                    <form>
                        <div class="mb-3">
                            <label for="signupName" class="form-label">Full Name</label>
                            <input type="text" class="form-control" id="signupName" placeholder="Enter your name">
                        </div>
                        <div class="mb-3">
                            <label for="signupEmail" class="form-label">Email Address</label>
                            <input type="email" class="form-control" id="signupEmail" placeholder="Enter your email">
                        </div>
                        <div class="mb-3">
                            <label for="signupPassword" class="form-label">Password</label>
                            <input type="password" class="form-control" id="signupPassword"
                                placeholder="Create a password">
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-custom">Sign Up</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
