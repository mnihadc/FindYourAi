<html>

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/svg+xml"
    href="data:image/svg+xml,%3Csvg%20xmlns%3D'http%3A//www.w3.org/2000/svg'%20viewBox%3D'0%200%2048%2048'%3E%3Cpath%20fill-rule%3D'evenodd'%20clip-rule%3D'evenodd'%20d%3D'M24%204H42V17.3333V30.6667H24V44H6V30.6667V17.3333H24V4Z'%20fill%3D'currentColor'%3E%3C/path%3E%3C/svg%3E">
  <title>{{title}}</title>
  {{#if getLoginpage}}
  <link rel="stylesheet" href="/css/login.css">
  {{/if}}
  <style>
    /* General reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;

    }

    /* Fixed header style */
    header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      height: 5rem;
      background-color: #111122;
      border-bottom: 2px solid #242447;
      padding: 10px 20px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
    }

    /* Header content */
    .logo-container {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .logo-container svg {
      width: 30px;
      height: 30px;
      fill: white;
    }

    .logo-container h2 {
      color: white;
      font-size: 1.2rem;
      font-weight: bold;
    }

    .header-right {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      gap: 20px;
    }

    .header-right a,
    .header-right button {
      color: white;
      text-decoration: none;
      font-size: 0.9rem;
    }

    .header-right button {
      background-color: #242447;
      padding: 5px 10px;
      border-radius: 10px;
    }

    .header-right a:hover,
    .header-right button:hover {
      background-color: #333;
    }

    /* Add padding to the body content to prevent overlap with the fixed header */
    body {
      padding-top: 55px;
      /* Adjust this based on header height */
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .header-right {
        gap: 10px;
      }

      .header-right a,
      .header-right button {
        font-size: 0.8rem;
      }
    }

    /* Dropdown Menu */
    .dropdown {
      position: relative;
      display: inline-block;
    }

    .dropdown-menu {
      position: absolute;
      right: 0;
      background-color: #242447;
      min-width: 150px;
      display: none;
      flex-direction: column;
      border-radius: 10px;
      overflow: hidden;
    }

    .dropdown:hover .dropdown-menu {
      display: flex;
    }

    .dropdown-menu a {
      padding: 10px;
      text-decoration: none;
      color: white;
      font-size: 0.9rem;
    }

    .dropdown-menu a:hover {
      background-color: #333;
    }
  </style>
</head>

<body>
  <header>
    <div class="logo-container">
      <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg">

        <path fill-rule="evenodd" clip-rule="evenodd" d="M24 4H42V17.3333V30.6667H24V44H6V30.6667V17.3333H24V4Z"
          fill="currentColor"></path>
      </svg>
      <a href="/">
        <h2>FindYourAI</h2>
      </a>
    </div>
    <nav class="header-right">
      <a class="text-white text-sm font-medium leading-normal" href="#" aria-label="About Page">About</a>
      {{#if user}}
      <a class="nav-link" href="/profile/user">
        <img src="/images/user.svg" alt="User" class="icon-small">
      </a>
      {{else}}
      <a class="text-white text-sm font-medium leading-normal" href="/auth/login" aria-label="Login Page">Login</a>
      {{/if}}
      <!-- Help Button with Dropdown -->
      <div class="dropdown">
        <button class="flex items-center justify-center rounded-xl h-10 px-4 bg-[#242447] text-white text-sm font-bold">
          Help
        </button>
        <ul class="dropdown-menu hidden absolute bg-[#242447] text-white text-sm shadow-lg rounded-xl mt-2">
          <li><a href="#" class="block px-4 py-2 hover:bg-[#333]">FAQs</a></li>
          <li><a href="#" class="block px-4 py-2 hover:bg-[#333]">Contact Support</a></li>
          <li><a href="#" class="block px-4 py-2 hover:bg-[#333]">Feedback</a></li>
        </ul>
      </div>
    </nav>
  </header>

  <!-- Dynamic page content -->
  {{{body}}}
</body>

</html>